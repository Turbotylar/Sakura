resources:
  - ../base

patches:
  - patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: "<"
    target:
      kind: Deployment
      version: apps/v1
      name: sakura

namePrefix: dev-