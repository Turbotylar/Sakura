resources:
  - ../base

patches:
  - patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/1/value
        value: "<"
    target:
      kind: Deployment
      name: sakura

namePrefix: dev-